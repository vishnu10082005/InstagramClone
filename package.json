{
  "name": "instagram-webapp",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm i && npm start",
    "backend": "cd backend && npm i && mkdir -p data && curl --output data/test_db.sqlite https://kq-storage.s3.ap-south-1.amazonaws.com/Simulated_work_backend/milestone30.sqlite && node app.js",
    "test:serve": "cd frontend && npm i && npm run test:serve"
  },
  "dependencies": {
    "@react-oauth/google": "^0.12.1"
  }
}
